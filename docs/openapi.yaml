openapi: 3.1.0
info:
  version: 1.0.0
  title: <nodejs-hw-mongodb>
  license:
    name: Apache 2.0
    url: <http://www.apache.org/licenses/LICENSE-2.0.html>
  description: >
    <An application for working with a collection of contacts>
tags:
  - name: Students
    description: Operations about students.
  - name: Auth
    description: Auth operations.
servers:
  - url: <http://localhost:3000>
  - url: <https://nodejs-hw-mongodb-e18j.onrender.com> #посилання на задеплоєний сайт 
paths:
  /contacts:
    post:
      $ref: ../swagger/paths/contacts/post.yaml #додаємо ще один шлях
  /contacts/{id}:
    get:
      $ref: ../swagger/paths/contacts/{id}/get.yaml
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
